# Mark for Review Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bluebook-style Mark for Review + Check Your Work page before module submit.

**Spec (binding):** `docs/superpowers/specs/2026-07-07-mark-for-review-design.md`

**Files:** Modify `app/hooks/useTestSession.ts`, `app/components/TestScreen.tsx`, `app/components/QuestionNavigator.tsx`, `app/components/QuestionView.tsx` (only if the last-question Next needs a new prop path), `CLAUDE.md`; Create `app/components/CheckYourWork.tsx`.

## Task 1: The whole feature (single task — one implementer)

- [ ] **Hook** (`useTestSession` — read it in FULL first; it carries the break phase, module-2 retry, stopwatch, and validity guards from #15): add `marked: Set<string>` (key `"${secIdx}-${modIdx}-${qIdx}"`), `toggleMarked()`, `moduleReview: boolean`, `openModuleReview()`, `closeModuleReview(qi?: number)`. Reset `marked` in `start()`/`newTest()`. **Review auto-close is ONE effect** (do NOT scatter per-site resets — the advances live in five different paths incl. the async Module-2 draw and `resumeFromBreak`): `useEffect(() => { setModuleReview(false); }, [secIdx, modIdx, screen]);`. **Stopwatch (the ONLY permitted timer-adjacent change, REQUIRED):** add `moduleReview` to the #15 display-effect guard and dep array (`if (screen !== 'test' || paused || moduleReview) return;`) so opening review commits+stops the stopwatch — review time must not accrue to the last-displayed question (it would corrupt pacing analytics). The COUNTDOWN interval, `handleTimeUp`, `submitModule`, break machinery: untouched. Do NOT call `submitModule` from anywhere new. Do NOT confuse the new `moduleReview` with the existing `showReview`/`toggleReview` (that one is the post-test results review).
- [ ] **TestScreen**: header row gains the "Mark for Review" bookmark toggle (filled amber when marked, `aria-pressed`; the term "flag" must not appear). When `moduleReview` is true render `<CheckYourWork>` INSTEAD of QuestionView + QuestionNavigator (TopBar with the timer stays). Pass `marked`/`onToggleMarked`/review handlers through. The Enter-key/global handlers, eliminator, calculator/reference panels, pause overlay, and module-2 error overlay must be untouched and must still work on the question view.
- [ ] **QuestionNavigator**: marked squares get a small bookmark corner badge (absolute-positioned mini SVG/glyph, `aria-label` includes "marked for review"); the submit `Button` becomes label "Review & submit" and calls `onOpenReview` (new prop replacing the direct `onSubmitModule` — the component no longer submits directly).
- [ ] **Last-question Next** opens review: in `TestScreen`, wrap `onNext`: last question → `openModuleReview()`, else the normal `onNext`. REQUIRED QuestionView edit (not conditional): the Next button is currently `disabled={isLast}` with label "Last question" — a disabled button fires no onClick, so the wrapper alone silently no-ops. Remove the `disabled={isLast}` and change the last-question label to "Review".
- [ ] **CheckYourWork.tsx** (new, `'use client'`, presentational + callbacks): props `{ section, modIdx, sectionResponses, isMarked(qi): boolean, submitLabel, onJump(qi), onBack, onSubmit }` — pass an `isMarked` predicate closing over the Set + key prefix (don't leak the global key format into the component); `sectionResponses` is already module-scoped in TestScreen. Render per spec: heading, legend, grid (answered filled blue, unanswered hollow, marked = amber bookmark badge; states coexist), Back to question (secondary), submit (primary). Lift the `submitLabel` computation from QuestionNavigator into TestScreen (it already has `isLastSection`/`isLastModule`) and pass to BOTH components. Real `<button>`s; on mount, move focus to the heading (the PausedOverlay focus-management precedent) so keyboard/SR users aren't stranded.
- [ ] **CLAUDE.md**: one gotcha bullet under the Parity section (or a tiny #16 section): marks are UI-only/never persisted; "Mark for Review" vs "flag" terminology rule; review page is the only submit path but `submitModule` semantics unchanged; timer runs during review.
- [ ] Gates: `pnpm type-check`, `pnpm lint`, `pnpm build`. Commit: `git add -A -- app/hooks/useTestSession.ts app/components/TestScreen.tsx app/components/QuestionNavigator.tsx app/components/QuestionView.tsx app/components/CheckYourWork.tsx CLAUDE.md && git commit -m "feat(test): mark for review + check-your-work page before module submit"`. Do NOT push.

Manual-smoke list (orchestrator): mark → navigate → review → jump back → submit; last-question "Review" button; time-up mid-review auto-submits and closes review; pause overlay covers the review page; Module 2 never starts under a stale review page; break entry closes review; short + full tests; stopwatch attributes no time to any question while review is open.
